/**
 * Legacy full seed script.
 * We now handle seeding incrementally.
 * Use commands:
 * `npm run seed:stages` -> seeds pipeline stages
 * `npm run seed:admin`  -> seeds admin users
 */
console.log('Legacy seed script disabled.');
console.log('Use `npm run seed:all` to seed default Pipeline Stages and Admin users.');
