import LeadGenLayout from '@/features/leadgen/layout/LeadGenLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <LeadGenLayout>{children}</LeadGenLayout>;
}
